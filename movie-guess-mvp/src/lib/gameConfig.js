// Tier 0 = hard, 1 = medium, 2 = easy — matches the clue order in movies.json.
export const TIER_DURATIONS_SEC = [30, 20, 20];
export const TIER_POINTS = [100, 60, 30];
export const ROUND_RESULT_PAUSE_MS = 6000;
export const ROUNDS_PER_GAME = 10;
